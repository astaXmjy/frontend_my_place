import 'package:flutter/material.dart';
class CreatedPlaceScreen extends StatelessWidget {
  const CreatedPlaceScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Created Place Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
